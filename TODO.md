## 功能优化与待办清单

### 1. 远程页面增加搜索功能
- [ ] **远程仓库最顶部（星标左侧）新增「搜索」按钮**：点击进入搜索页面
  - 搜索框下方显示「近期搜索」（最右侧加「清除」按钮，点击清空全部历史）
  - 近期搜索下方显示搜索历史列表，点击任意历史可直接填入搜索框
  - 支持回车搜索 + 点击搜索按钮
  - 输入内容后下方实时显示分类列表：
    - 具有 "..." 的代码
    - 包含 "..." 的仓库
    - 包含 "..." 的议题
    - 包含 "..." 的拉取请求
    - 包含 "..." 的人员
    - 包含 "..." 的组织
  - 选中分类后执行对应搜索；不选则直接搜索全部

---

### 2. 仓库详情页面

#### 2.1 Tab 交互优化
- [ ] **文件、提交、分支、操作、发行版、PR 和 Issues 等 Tab 支持左右滑动切换**
- [ ] **提交 Tab 和操作 Tab 增加点击状态反馈**
  - 当前在提交 Tab 和操作 Tab进行点击操作时缺少视觉反馈
  - 需要添加点击涟漪效果（Ripple Effect）或背景色变化
  - 提升用户交互体验，明确告知用户操作已被识别

#### 2.2 各 Tab 内搜索功能优化
- [ ] **Issues Tab**：增加搜索功能
- [ ] **Files Tab**：
  - 左侧按钮区域新增「搜索」按钮
  - 点击后搜索当前 Tab 内容
  - 搜索结果样式完全参考 GitHub 代码搜索效果

- [ ] **搜索筛选方案决策（需确认）**
  - 方案一：顶部操作栏根据当前 Tab 动态切换搜索
    - 优点：界面统一，不占用内容区域
    - 缺点：用户需注意当前 Tab 与搜索范围的对应关系
  - 方案二：每个 Tab 下方增加独立搜索筛选框
    - 优点：意图清晰，操作直观
    - 缺点：占用内容区域，界面略复杂
  - **搜索交互行为**：
    - 输入方式：输入后自动搜索 OR 回车搜索（待确认）
    - 搜索历史：所有搜索页面保留搜索记录
    - 一键清除：支持快速清空搜索条件
  > 建议优先明确交互行为与方案选择，确保搜索体验在各 Tab 间保持一致。

---

### 3. PR 功能完善
- [ ] **PR 功能**：当前 PR 不支持点击查看进行相关代码操作。

---

### 4. 本地卡片菜单新增功能
- [ ] **卡片按钮菜单中「重新扫描」上方新增「归一」按钮**
  - 点击后弹出提交信息输入窗口
  - 功能：删除仓库所有提交历史（squash / reset），然后进行一次新的提交

---

### 5. 性能优化
- [ ] **加快启动速度**：启动页显示时间过长（目前 1~2 秒），优化启动流程，目标显著缩短启动时间

---

### 6. 安全与本地仓库管理
- [ ] **Token 加密问题**：授权登录后产生的 `gitmob_prefs.preferences_pb` 中 `access_token` 为明文，需实现加密存储

---

### 7. Git 底层改造：自包含 libgit2 + git24j
- [ ] 编译原生库：
  - [ ] 从 PuppyGit 项目复制 `lib_build_scripts/` 到本工程根目录
  - [ ] 配置 NDK r27，执行 `3_buildlibs.sh` 生成各 ABI 的 .so 文件
  - [ ] 将 .so 文件放入 `app/src/main/jniLibs/{abi}/`
  - [ ] 将 `git24j.jar` 放入 `app/libs/`，并在 build.gradle 中依赖
  - [ ] 添加 CMakeLists.txt（如需要）

---

### 8. 仓库列表获取策略优化（技术难点）

**问题描述：**
当前仓库列表获取面临性能与完整性的权衡问题。

**方案对比：**

#### 方案一：全量获取（当前可能的实现）
- **优点：**
  - 可以获取用户所有仓库的完整语言信息
  - 语言筛选时数据准确，语言列表完整不会缺失
  - 筛选结果包含所有匹配的仓库
- **缺点：**
  - 用户若有上千个仓库，获取时间会非常长
  - 可能导致应用卡死或 ANR（Application Not Responding）
  - 首次加载体验差，用户等待时间过长
  - 占用大量内存

#### 方案二：分页获取
- **优点：**
  - 加载速度快，用户体验流畅
  - 内存占用可控
  - 支持下拉刷新和上拉加载更多
- **缺点：**
  - 仓库获取不全，只能获取已加载的页面数据
  - 语言列表可能不完整，只包含当前已加载仓库的语言
  - 按语言筛选时结果不准确，可能遗漏未加载页面中的匹配仓库
  - 用户需要手动加载更多才能看到全部数据

**可能的解决方案：**

- [ ] **混合策略**：
  - 首次加载使用分页获取（快速展示）
  - 后台异步继续获取剩余仓库数据
  - 实时更新语言列表和筛选结果
  - 添加加载进度提示（如"已加载 200/1500 个仓库"）

- [ ] **智能缓存**：
  - 缓存已获取的仓库数据和语言信息
  - 定期后台同步更新
  - 减少重复请求

- [ ] **语言列表预加载**：
  - 使用 GitHub API 的 `/user/repos` 接口配合 `per_page=100` 参数
  - 优先获取前几页数据构建初步语言列表
  - 提供"加载完整语言列表"选项供用户手动触发

- [ ] **写死 GitHub 所有语言列表**：
  - 在应用中硬编码 GitHub 支持的所有编程语言列表
  - 参考 GitHub Linguist 项目的语言定义：
    - https://github.com/github/linguist/blob/master/lib/linguist/languages.yml 
    - https://www.npmjs.com/package/linguist-languages?activeTab=code
  - 优点：
    - 语言筛选列表始终完整，不受已加载仓库影响
    - 无需等待数据加载即可显示完整语言选项
    - 用户体验一致，不会出现语言列表不全的情况
  - 缺点：
    - 需要定期更新语言列表以包含新增语言
    - 可能显示用户实际没有使用的语言（可通过灰色显示或隐藏未使用语言优化）
    - 筛选时仍需加载对应语言的所有仓库（可能触发全量加载）
  - 实现建议：
    - 将常用语言（Top 50）置顶显示
    - 对用户已使用的语言添加标记或高亮
    - 提供"仅显示已使用语言"开关

- [ ] **筛选模式切换**：
  - 提供"快速模式"（仅筛选已加载数据）
  - 提供"完整模式"（触发全量加载后筛选）
  - 让用户根据需求选择

> **建议：** 优先实现"写死语言列表 + 混合策略"的组合方案，即使用完整的 GitHub 语言列表作为筛选选项，同时采用分页加载 + 后台异步获取的方式逐步加载仓库数据，并在 UI 上明确提示当前数据加载状态和筛选结果的完整性。